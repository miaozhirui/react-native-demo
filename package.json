{
  "name": "zixunjilu",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "JS_DIR=`pwd`/ReactComponent; cd node_modules/react-native; npm run start -- --root $JS_DIR",
    "pod": "pod install --verbose --no-repo-update",
    "rewatchman": "watchman shutdown-server && brew reinstall watchman"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-native": "^0.19.0"
  }
}
